apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  type: ClusterIP
  # Routes to pods from redis-deployment
  selector:
    app.kubernetes.io/name: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
