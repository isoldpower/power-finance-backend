# See ../../README.md → "AI-side Postgres"
include_if_exists = '/var/lib/postgresql/data/pgdata/postgresql.conf'

wal_level = logical
max_wal_senders = 4
max_replication_slots = 4
max_slot_wal_keep_size = '2GB'
